#   includes/templates/templ_tsa.yaml
#template:
  - binary_sensor:
      - name: "tsa_gist"
        state: "{{ states('sensor.temperature_ts1') | float > states('input_number.in_box_tsa') | float  }}"
